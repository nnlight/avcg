
1) координаты в statusbar
2) доделать FindDialog
3) в файле есть начальный scaling - надо его подцеплять



баги:

0) сам cfg.vcg неправильно показывается
1) если открыть файл с "угловатыми" стрелками (cfg.vcg), а потом обычный visual, то угловатось залипает
2) само рисование надо делать не в delayed_zoom, а в expose_event (иначе время потраченное на рисование уходит впустую, т.к. на эране ничего не появляется (если уже успели прийти следующие scroll-events))



!!
